<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Web Dev Projects | HackStack</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background-color: #121212;
      color: #f1f1f1;
      font-family: 'Segoe UI', sans-serif;
    }

    h2 {
      color: #fff;
    }

    ul.project-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    ul.project-list li {
      padding: 1.2rem;
      margin-bottom: 1rem;
      border: 1px solid #2a2a2a;
      border-radius: 8px;
      background-color: #1e1e1e;
      transition: border-color 0.3s;
    }

    ul.project-list li:hover {
      border-color: #444;
    }

    .project-title {
      font-size: 1.2rem;
      font-weight: 600;
      color: #ffffff;
    }

    .project-description {
      font-size: 0.95rem;
      color: #bbbbbb;
      margin: 0.5rem 0;
    }

    .view-link {
      color: #0d6efd;
      text-decoration: none;
      font-weight: 500;
    }

    .view-link:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container py-5">
    <h2 class="mb-4 text-center"><%= categories.title %> Projects</h2>
    <ul class="project-list">
      <% for(let i=0; i<categories.projects.length; i++) { %>
        <li>
          <div class="project-title"><%= categories.projects[i].title %></div>
          <p class="project-description">
            <%= categories.projects[i].description %>
          </p>
          <form action="/category/<%= categories._id %>/projects/<%= categories.projects[i].projectId %>" method="GET" class="mt-auto">  
            <button class="btn btn-outline-secondary mt-auto">View Details →</button>
          </form>
        </li>
      <% } %>
    </ul>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>


